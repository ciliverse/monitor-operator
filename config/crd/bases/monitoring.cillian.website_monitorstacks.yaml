---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: monitorstacks.monitoring.cillian.website
spec:
  group: monitoring.cillian.website
  names:
    kind: MonitorStack
    listKind: MonitorStackList
    plural: monitorstacks
    singular: monitorstack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.prometheusStatus.ready
      name: Prometheus
      type: boolean
    - jsonPath: .status.grafanaStatus.ready
      name: Grafana
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: MonitorStack is the Schema for the monitorstacks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: 期望状态 - 用户定义的配置
            properties:
              grafana:
                description: Grafana配置
                properties:
                  adminPassword:
                    default: admin
                    description: 管理员密码
                    type: string
                  dashboards:
                    description: 仪表板配置
                    items:
                      description: DashboardSpec defines Grafana dashboard
                      properties:
                        json:
                          type: string
                        name:
                          type: string
                        url:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  datasources:
                    description: 数据源配置
                    items:
                      description: DatasourceSpec defines Grafana datasource
                      properties:
                        name:
                          description: 数据源名称
                          type: string
                        type:
                          type: string
                        url:
                          type: string
                      required:
                      - name
                      - type
                      - url
                      type: object
                    type: array
                  enabled:
                    default: true
                    description: 是否启用Grafana
                    type: boolean
                  image:
                    default: grafana/grafana
                    description: 镜像配置
                    type: string
                  resources:
                    description: 资源配置
                    properties:
                      limits:
                        description: ResourceList defines CPU and memory resources
                        properties:
                          cpu:
                            type: string
                          memory:
                            type: string
                        type: object
                      requests:
                        description: ResourceList defines CPU and memory resources
                        properties:
                          cpu:
                            type: string
                          memory:
                            type: string
                        type: object
                    type: object
                  service:
                    description: 服务配置
                    properties:
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      nodePort:
                        format: int32
                        maximum: 32767
                        minimum: 30000
                        type: integer
                      port:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      type:
                        default: ClusterIP
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        - ExternalName
                        type: string
                    type: object
                  tag:
                    default: latest
                    type: string
                required:
                - enabled
                type: object
              labels:
                additionalProperties:
                  type: string
                description: 资源标签
                type: object
              namespace:
                type: string
              prometheus:
                description: |-
                  foo is an example field of MonitorStack. Edit monitorstack_types.go to remove/update
                  Prometheus配置
                properties:
                  config:
                    description: 配置文件
                    type: string
                  enabled:
                    description: 是否启用Prometheus
                    type: boolean
                  image:
                    default: prom/prometheus
                    description: 镜像配置
                    type: string
                  resources:
                    description: 资源配置
                    properties:
                      limits:
                        description: ResourceList defines CPU and memory resources
                        properties:
                          cpu:
                            type: string
                          memory:
                            type: string
                        type: object
                      requests:
                        description: ResourceList defines CPU and memory resources
                        properties:
                          cpu:
                            type: string
                          memory:
                            type: string
                        type: object
                    type: object
                  retention:
                    default: 15d
                    description: 数据保留时间
                    pattern: ^[0-9]+[smhdy]$
                    type: string
                  service:
                    description: 服务配置
                    properties:
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      nodePort:
                        format: int32
                        maximum: 32767
                        minimum: 30000
                        type: integer
                      port:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      type:
                        default: ClusterIP
                        enum:
                        - ClusterIP
                        - NodePort
                        - LoadBalancer
                        - ExternalName
                        type: string
                    type: object
                  storage:
                    description: 存储配置
                    properties:
                      size:
                        type: string
                      storageClass:
                        type: string
                    type: object
                  tag:
                    default: latest
                    type: string
                required:
                - enabled
                type: object
            required:
            - grafana
            - prometheus
            type: object
          status:
            description: 观察状态 - 控制器维护的实际状态
            properties:
              conditions:
                description: 条件列表 - 详细的状态条件
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              grafanaStatus:
                description: Grafana组件状态
                properties:
                  endpoint:
                    description: 服务端点 - 可访问的服务地址
                    type: string
                  message:
                    description: 状态消息
                    type: string
                  ready:
                    type: boolean
                  replicas:
                    description: 副本数量
                    format: int32
                    type: integer
                required:
                - ready
                type: object
              lastUpdated:
                description: 最后更新时间
                format: date-time
                type: string
              message:
                description: 状态消息 - 详细的状态描述
                type: string
              phase:
                description: |-
                  conditions represent the current state of the MonitorStack resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                  整体状态 - Pending, Ready, Failed等
                enum:
                - Pending
                - Ready
                - Failed
                - Updating
                type: string
              prometheusStatus:
                description: Prometheus组件状态
                properties:
                  endpoint:
                    description: 服务端点 - 可访问的服务地址
                    type: string
                  message:
                    description: 状态消息
                    type: string
                  ready:
                    type: boolean
                  replicas:
                    description: 副本数量
                    format: int32
                    type: integer
                required:
                - ready
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
